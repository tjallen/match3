// sample for gulp-babel
const person = { name: 'Thom Allen' };
const template = `Hello, ${person.name}`;

const someUnusedShit = 123;
const moreUnused = [
  1,
  2,
  3
];

const shit = { unused: 1, shit: 2, };

console.log(template);

import 'styles.scss';
